/**
 * @module
 * JSX parser for markuplint. Provides a parser that transforms JSX/TSX source code into
 * markuplint's AST by leveraging the TypeScript ESTree parser to extract JSX elements.
 */

export { parser } from './parser.js';
