/**
 * Re-export of the `glob` function from the `glob` package.
 * Provides file system globbing capabilities for locating test fixtures and source files in tests.
 */
export { glob } from 'glob';

/**
 * Re-export of the `execa` function from the `execa` package.
 * Provides a convenient API for spawning child processes in tests, such as running CLI commands.
 */
export { execa } from 'execa';
