{
	"name": "@markuplint/isolated-env-test-ts",
	"version": "4.0.0",
	"main": "index.js",
	"author": "yusukehirao",
	"license": "MIT",
	"private": true,
	"type": "module",
	"scripts": {
		"test": "npm-run-all --parallel test:*",
		"test:build": "tsc --build --dry",
		"test:normal": "tsc --noEmit",
		"test:ts-node": "node --loader ts-node/esm index.ts"
	},
	"dependencies": {
		"typescript": "latest",
		"ts-node": "latest"
	},
	"devDependencies": {
		"npm-run-all": "latest"
	}
}
